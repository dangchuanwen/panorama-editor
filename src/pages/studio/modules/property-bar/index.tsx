import React from 'react';
import { Box } from '@material-ui/core';

const PropertyBar: React.FC = () => {
  return (
    <Box height="100%" style={{ backgroundColor: 'gray' }}>
      属性列表
    </Box>
  );
};

export default PropertyBar;
